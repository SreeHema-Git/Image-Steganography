# Image-Steganography
Image Steganography Project in C is a console application that hides and retrieves secret data in BMP images using LSB technique. It supports encoding and decoding via command-line options, validates files, handles binary data safely, and preserves image quality while securely embedding information.
